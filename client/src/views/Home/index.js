import React from "react"
import Alert from '../../shared/Notifications/Alert'

const Home = props => (
  <div>
    <h1>Home Page</h1>
    {<Alert strMessage="rea" alertText="text" alertType="error" />}
  </div>
)

export default Home
